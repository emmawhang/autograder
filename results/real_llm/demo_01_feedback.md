# Autograder Feedback — Demo 01

**Total score:** 95 / 100

Summary:
- Student submission is largely correct. Data loaded and parsed, predictors constructed, and both IS and OOS R² calculated.
- Minor formatting issues in the final table (reporting format differs slightly).

Strengths:
- Correct lag handling and predictor construction.
- Correct benchmark (historical mean) usage.
- OOS rolling window implemented as specified.

Suggestions:
- Format the final table values consistently as percent (e.g., 5.43 not 0.0543).
- Ensure final table column labels match exact rubric names (`IS_R2_head`, `OOS_R2_head`).

Note: This file is a local mock output for demo purposes. For real grading, run the autograder script which will call the LLM and produce live results.
